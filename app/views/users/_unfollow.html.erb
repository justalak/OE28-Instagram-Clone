<%= form_for get_relationship(user), html: {id: user.id, method: :delete }, remote: true do |f| %>
  <% if get_relationship(user).accept? %>
    <%= f.submit t(".unfollow"), class: "btn btn-danger" %>
  <% else %>
    <%= f.submit t(".cancel"), class: "btn btn-info" %>
  <% end %>
<% end %>
