<% @update[:notifications].each do |notif| %>
  var notifId = 'notification_'.concat('<%= notif.id.to_s %>');
  $('#' + notifId).find('.badge-confirm-cancel')
    .html('<div class="badge badge-primary text-wrap confirmed_refused rounded-0 "><%= @update[:message_confirmed] %></div>');
<% end %>
