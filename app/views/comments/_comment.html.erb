<div class="comments-block" id="comment-<%= comment.id %>">
  <%= render "comments/comment_detail", comment: comment %>
  <%= render partial: "comments/comment_detail", collection: comment.replies,
    as: :comment %>
</div>
