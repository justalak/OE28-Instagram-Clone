$('#frame-notification')
  .append('<%= j render partial: "notifications/notification", collection: @notifications %>');

var sizeof_notification = '<%= @notifications.size %>';
var previews_per_page = '<%= Settings.user.previews_per_page %>';
if(parseInt(sizeof_notification) < parseInt(previews_per_page)) {
  $('#load-notifications').hide();
}
