<%= form_for relationship, url: relationship_path(relationship),
  html: {class: "delete_form_submit", method: :delete },
  remote: true do |f| %>
  <%= f.submit t(".refuse"),
    class: "button_delete_notif btn btn-outline-danger btn-sm refuse_request" %>
<% end %>
