$('div#bookmark_like__like')
  .html('<%= j render("likes/unlike") %>');
$('div#count_like_post')
  .html('<%= j link_to(t(".count_like", count: User.likers(@post.id).size), likers_post_path(@post)) %>');
